[package]
name = "healm"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = { version = "1", default-features = false, optional = true }

[dev-dependencies]
paste = "1"
rand = "0.8"

[features]
test = []
